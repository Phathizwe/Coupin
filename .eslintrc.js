module.exports = {
  // Extend existing configuration
  extends: [
    // Your existing extends...
  ],
  // Override rules if needed
  rules: {
    // Your rules...
  },
  // Explicitly include only .tsx and .jsx files
  ignorePatterns: ['**/*.ts', '!**/*.tsx', '!**/*.jsx'],
};