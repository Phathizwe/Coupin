import React from 'react';
import QRCodeLayout from '../../components/qr-code/QRCodeLayout';

/**
 * Enhanced QR Code Generator page with emotional design
 * Uses the same data hooks and preserves all existing functionality
 */
const EnhancedQRCodePage: React.FC = () => {
  return <QRCodeLayout />;
};

export default EnhancedQRCodePage;